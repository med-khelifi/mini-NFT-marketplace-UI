class TopSeller {
  String title;
  String subtitle;
  String imagePath;
  double rate;
  int reacts;

  TopSeller({required this.title,required this.subtitle,required this.rate,required this.reacts,required this.imagePath,});
}